import React from 'react';
import './UserProfile.css';

const UserProfile = () => {
    return (
        <div>
            <h1>This is user profile....</h1>
        </div>
    );
};

export default UserProfile;